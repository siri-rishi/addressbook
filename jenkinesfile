pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                script {
                echo "building the code"
            }
        }
    }
    stage("Compile") {
        steps {
            script {
                echo "Compiling the code"
            }
                
        }
        }
    }
    }
